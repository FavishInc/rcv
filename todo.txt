/**
 * check that each ballot doesn't have duplicate candidates
 * remove candidates and votes from constructor
 * setCandidates method
 * addVote method
 * candidate property on instance
 * getResults()
 * options for filterCandidates
 * addCandidate
 * candidates are optionally added by votes themselves
 * setVotes
 * isOpen option
 **/